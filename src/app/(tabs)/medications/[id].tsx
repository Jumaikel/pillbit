/**
 * Medication Detail Route
 * Route: /medications/[id]
 *
 * Thin route file — delegates all logic and UI to the feature screen.
 * The dynamic segment `id` is passed as a route param.
 */

import { MedicationDetailScreen } from '@/features/medication';

export default MedicationDetailScreen;
